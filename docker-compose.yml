version: "3"
services:
  neptune-ui:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - ui-service
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=development
    restart: unless-stopped

networks:
  neptune-ui:
    driver: bridge
